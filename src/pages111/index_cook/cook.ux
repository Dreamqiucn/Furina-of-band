<template>
  <div class="page download-page" @swipe="toHomePage">
    <div class="header column center">
      <text class="a">烹饪</text>
      <text class="c">致水神还有 {{ uncake }} 个</text>
      <text class="b">致水神10个 => 普茹斯蒂司1个</text>
    </div>
    <div class="body column center">
      <image class="icon" src="/common/icon/uncake.png"></image>
    </div>
    <div class="footer column center">
      <div style="margin-top: 10px">
        <input
          class="but"
          type="button"
          value="制作"
          style="font-size: 30px; color: #ffffff"
          onclick="eatCake"
        />
      </div>
    </div>
    <div class="header column center">
      <text class="c">甜甜花和鸡腿还有 {{ food }} 个</text>
      <text class="c">现在有 {{ j }} 个甜甜花酿鸡</text>
      <text class="b">原料2个=>甜甜花酿鸡1个</text>
    </div>
    <div class="body center">
      <image class="icon2" src="/common/icon/flower.png"></image>
      <image class="icon2" src="/common/icon/j.png"></image>
    </div>
    <div class="footer column center">
      <div style="margin-top: 10px">
        <input
          class="but"
          type="button"
          value="制作"
          style="font-size: 30px; color: #ffffff"
          onclick="cook"
        />
      </div>
    </div>
  </div>
</template>

<style>
.back {
  position: absolute;
  left: 6px;
  top: 6px;
  width: 72px;
  height: 72px;
  opacity: 1;
  z-index: 1000;
}

.description {
  background-color: rgba(255, 255, 255, 0.2);
  color: #fff;
  border-radius: 20px;
  padding: 30px;

  margin: 0 auto;
  /* Center align */
}

.icon {
  width: 156px;
  height: 156px;
}

.but {
  width: 80%;
  background-color: rgba(255, 255, 255, 0.2);
  border-radius: 30px;
}

.download-page {
  display: flex;
  flex-direction: column;

  height: 100%;
  position: absolute;
  width: 100%;
}

text {
  color: #ffffff;
}

.column {
  /* 竖直 */
  flex-direction: column;
}

.row {
  /* 水平 */
  flex-direction: row;
}

.center {
  align-items: center;
  justify-content: center;
}

.header {
  margin-top: 10px;
}

.page {
  /* 页面大小 */

  background-color: #000000;
  width: 383px;
  height: 480px;
}

.body {
  margin-top: 10px;
  margin-left: 15px;
}

.footer {
  margin-top: 10px;
}

.icon2 {
  width: 256px;
  flex: auto;
  margin-top: 10px;
  margin-left: 0px;
}

.a {
  font-size: 55px;
}

.b {
  font-size: 30px;

  color: #9b9b9b;
}

.c {
  font-size: 30px;
  color: #d8d8d8;
}

.d {
  font-size: 20px;
  color: #adadad;
  margin-top: 10px;
}

.e {
  font-size: 20px;
  color: #ffffff;
  margin-left: 10px;
}
</style>
<script>
// 导入模块
import router from "@system.router"
import storage from "../../common/storage.js"
import prompt from "@system.prompt"

export default {
  public: {
    cake: 0,
    level: 0,
    uncake: 0,
    food: 0,
    j: 0
  },
  eatCake() {
    storage.get({
      key: "uncake",
      success: (resca) => {
        this.uncake = resca ? parseInt(resca, 10) : 0
        if (this.uncake < 10) {
          return
        }
        this.uncake -= 10
        storage.set({
          key: "uncake",
          value: this.uncake.toString()
        })
        storage.get({
          key: "cake",
          success: (resA) => {
            this.cake = resA ? parseInt(resA, 10) : 0
            this.cake += 1
            storage.set({
              key: "cake",
              value: this.cake.toString()
            })
          }
        })
      }
    })
  },
  cook() {
    storage.get({
      key: "food",
      success: (rescad) => {
        this.food = rescad ? parseInt(rescad, 10) : 0
        if (this.food < 2) {
          return
        }
        this.food -= 2
        storage.set({
          key: "food",
          value: this.food.toString()
        })
        storage.get({
          key: "j",
          success: (resB) => {
            this.j = resB ? parseInt(resB, 10) : 0
            this.j += 1
            storage.set({
              key: "j",
              value: this.j.toString()
            })
          }
        })
      }
    })
  },
  onInit() {
    storage.get({
      key: "index",
      success: (data) => {
        this.aaa = data
      }
    })
    storage.get({
      key: "rpg",
      success: (data) => {
        console.log(data)
        if (data == 721 || this.aaa == "1234567890") {
          console.log("successful")
        } else if (data == "") {
          console.log("无设备码")
          prompt.showToast({
            message: "盗版死妈操你妈",
            duration: 1000
          })
          router.replace({
            uri: "pages/lock"
          })
        } else {
          console.log("有设备码没验证")

          prompt.showToast({
            message: "盗版死妈操你妈",
            duration: 1000
          })
          router.replace({
            uri: "pages/lock"
          })
        }
      },
      fail: () => {}
    })
    storage.get({
      key: "cake",
      success: (resca) => {
        this.cake = resca ? parseInt(resca, 10) : 0
      }
    })
    storage.get({
      key: "j",
      success: (resca) => {
        this.j = resca ? parseInt(resca, 10) : 0
      }
    })
    storage.get({
      key: "food",

      success: (resca) => {
        this.food = resca ? parseInt(resca, 10) : 0
      }
    })
    storage.get({
      key: "uncake",
      success: (resca) => {
        this.uncake = resca ? parseInt(resca, 10) : 0
      }
    })
  },
  toHomePage(eve) {
    if (eve.direction === "right") {
      router.replace({
        uri: "/pages/index"
      })
    }
  },
  back() {
    router.replace({
      uri: "/pages/index"
    })
  }
}
</script>
